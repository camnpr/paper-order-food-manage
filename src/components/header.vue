<template>
  <el-menu
    :default-active="activeIndex"
    background-color="#082c61"
    active-text-color="#FFFAF0"
    text-color="#fff"
    class="outer-header"
    mode="horizontal"
    :ellipsis="false"
    @select="handleSelect"
  >
    <el-menu-item index="0" @click="open('operation')" class="header-title">网上订餐系统后台管理</el-menu-item>
    <div class="flex-grow1" />
    <div class="flex-grow1" />
    <el-icon>
     <Bell />
    </el-icon>
    <el-dropdown>
    <span class="el-dropdown-link">
      <el-icon>
      <Avatar />
    </el-icon>
    </span>
    <template #dropdown>
      <el-dropdown-menu>
        <el-dropdown-item @click="logout">退出</el-dropdown-item>
      </el-dropdown-menu>
    </template>
  </el-dropdown>
 
  </el-menu>
</template>

<script setup>
import { ref } from 'vue'
import { useRouter } from 'vue-router';

const router = useRouter();
const activeIndex = ref('1')
// const handleSelect = (key: string, keyPath: string[]) => {
//   console.log(key, keyPath)
// }

// 点击事件，路由跳转
let open = name => {
  router.push({name})
}
const logout = () => {
  router.replace('/login')
}


</script>

<style scoped>
.flex-grow {
  flex-grow: 0.3;
}
.flex-grow1 {
  flex-grow: 0.6;
}

:deep(.el-menu) {
  height: 46px;
  border-bottom: none;
}
:deep(.el-menu-item) {
  font-size: 10px;
}
:deep(.el-icon) {
 position: relative;
 font-size: 24px;
 color: rgb(255, 255, 255);
 margin: 10px;
}
.header-title {
  font-weight: 800;
  font-size: 18px;
  letter-spacing: 5px;
}

</style>
